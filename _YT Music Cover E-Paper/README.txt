1. Install the extension on Firefox "about:debugging#/runtime/this-firefox" and select "Load temp add-on", then select the manifest.json
2. Run the app.py script (after changing the IP address to the RPi's IP address)
3. Play a music on YouTube Music (URL in address bar)
4. It will be sent to the e-paper python server display (RPi)